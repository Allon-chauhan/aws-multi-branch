version: '3.8'
services:
  node-react-app:
    image: nateallon/demo-app:1.1.1-15
    ports:
      - 8080:8080

  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=password
